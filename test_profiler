from etl.profiler import (
    profile_csv,
    save_profile_report,
)

report = profile_csv("data/raw/customers.csv")

output_file = save_profile_report(
    report,
    "customers"
)

print(f"\nReport saved to: {output_file}")